services:
  - type: web
    name: mediagrab-api
    env: node
    buildCommand: npm install && pip install yt-dlp
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: 18